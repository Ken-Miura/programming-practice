Attrクラスをジェネリックとすることは、呼び出し側で値を取得する際にキャストが必要なくなるのでいいことだと考えらる。
Attributedインターフェースは、パラメータ化された特定の型のAttrのみしか保持できなくなる。
Attrクラスをジェネリックとすることは、Attributedオブジェクトにはパラメータ化された特定の型のAttrオブジェクトのみがコレクションとして保持されて、
コレクションの中に意図しない型が混ざることがないという点で意味がある。
