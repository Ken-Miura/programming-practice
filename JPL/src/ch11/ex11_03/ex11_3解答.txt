Attrクラスをジェネリックとすることは、呼び出し側で値を取得する際にキャストが必要なくなるのでいいことだと考えらる。
ネストしたstaticな型は、staticなメンバであるので型パラメータを使えず、Attributedインターフェースは、Attrクラスの外部に定義する必要が出てくる。
Attrクラスをジェネリックとすることは、Attributedオブジェクトに対して、パラメータ化された特定の型のAttrオブジェクトのみがコレクションとして保持されるという点で意味がある。
