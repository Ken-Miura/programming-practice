package ch10.ex10_02;

public class EscapeSequenceReplacement {
	// private static String replaceEscapeSequences(String source) {// int
	// length = source.length();// for (int i = 0; i < length; i++) {// switch
	// (source.substring(i, i + 1)) {// case "\b":// source =
	// source.replace("\b", BS + "b");// break;// case "\t":// source =
	// source.replace("\t", BS + "t");// break;// case "\n":// source =
	// source.replace("\n", BS + "n");// break;// case "\f":// source =
	// source.replace("\f", BS + "f");// break;// case "\r":// source =
	// source.replace("\r", BS + "r");// break;// case "\"":// source =
	// source.replace("\"", BS + "\"");// break;// case "\'":// source =
	// source.replace("\'", BS + "'");// break;// case "\\":// source =
	// source.replace("\\", BS+BS);// i++;// break;// default:// // do nothing//
	// }// }// return source;// }
}
